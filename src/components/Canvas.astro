---

---

<div class="fluid-layer" aria-hidden="true">
  <canvas id="fluid-canvas" class="fluid-canvas"></canvas>
</div>

<script>
  import { mountFluid } from "../libs/fluid";

  const canvas = document.querySelector<HTMLCanvasElement>("#fluid-canvas");

    console.log("Canvas element:", canvas);

  if (canvas) {
    mountFluid(canvas);
  }
</script>

<style>
  .fluid-layer {
    position: fixed;
    inset: 0;
    /* z-index: -1; */
    /* pointer-events: none; */
    /* overflow: hidden; */
    /* background:
      radial-gradient(circle at top, rgba(15, 23, 42, 0.22), transparent 45%),
      linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(224, 242, 254, 0.72)); */
  }

  .fluid-canvas {
    display: block;
    width: 100%;
    height: 100%;
    opacity: 0.95;
  }
</style>
